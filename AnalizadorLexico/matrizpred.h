#ifndef MATRIZPRED_H
#define MATRIZPRED_H

// Definición de la matriz como un recurso global
extern int matriz_pred[35][58];

// Declaración de la función para obtener un estado de la matriz
int obtenerProduccion(int prod, int col);

#endif // MATRIZPRED_H
